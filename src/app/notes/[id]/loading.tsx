//Makes the rendering faster as component
// can be streamed directly into the component
// instead of waiting for the entire page to load
// on a similar note

export default function Loading() {
  return <p>Loading...</p>;
}
